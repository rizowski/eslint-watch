#!/usr/bin/env node
'use strict';
const path = require('path');

const esw = require(path.join(__dirname, '..', 'build'));

esw
  .run(process.argv)
  .then(console.log)
  .catch((err) => {
    console.error(err);
    process.exit(1);
  });
