#!/usr/bin/env node
'use strict';
var path = require('path');
var pathNormalizer = require(path.join(__dirname, '../', 'src', 'path-normalizer'));
var eswPath = path.join(__dirname, '..', 'src', 'cli');

require(pathNormalizer.normalize(eswPath));
